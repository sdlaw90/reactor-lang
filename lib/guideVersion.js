// Bump this when the intro tour changes enough that re-showing it to existing
// users is worthwhile (e.g. a new practice mode joins the walkthrough).
// Tracked per-account as user_metadata.guide_seen_version, mirroring the
// welcome/what's-new "seen version" pattern.
export const GUIDE_VERSION = "1.0";
