// Bump this if a major enough change happens that re-showing the welcome
// popup to existing users would be worthwhile (e.g. a new practice mode).
// Tracked per-account as user_metadata.welcome_seen_version.
export const WELCOME_VERSION = "1.0";
